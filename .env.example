# PostgreSQL connection.
# Local Docker/Node example:
DATABASE_URL=postgresql://affirmative:affirmative@localhost:5432/affirmative_pill

# Local development can maintain a small pool.
DB_POOL_MAX=5

# worker = Docker projector.
# inline = Vercel/serverless.
PROJECTION_MODE=worker

PROJECTION_POLL_MS=700